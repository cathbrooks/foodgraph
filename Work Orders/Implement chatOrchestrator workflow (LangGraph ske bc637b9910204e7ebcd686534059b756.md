# Implement chatOrchestrator workflow (LangGraph skeleton)

Area: AI
Description: Wire a LangGraph flow that accepts a user query, calls slotResolver + restaurantProvider + scorer, and produces a structured response.
Sequence: 24
Status: Backlog